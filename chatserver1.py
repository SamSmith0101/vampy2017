#!/usr/bin/python3

import socket

address = ""
port = int(input("Enter a port to host on: "))

s = socket.socket()

s.bind((address, port))

s.listen

conn, address = 

while True:
	data = conn.recv
